<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImScheduledMessageMapper">

    <select id="selectPendingByUserId" resultType="com.echoim.server.entity.ImScheduledMessageEntity">
        SELECT *
        FROM im_scheduled_message
        WHERE user_id = #{userId}
          AND status = 1
        ORDER BY scheduled_at ASC
    </select>

    <select id="selectPendingByConversationId" resultType="com.echoim.server.entity.ImScheduledMessageEntity">
        SELECT *
        FROM im_scheduled_message
        WHERE conversation_id = #{conversationId}
          AND status = 1
        ORDER BY scheduled_at ASC
    </select>

    <select id="selectPendingBeforeTime" resultType="com.echoim.server.entity.ImScheduledMessageEntity">
        SELECT *
        FROM im_scheduled_message
        WHERE status = 1
          AND scheduled_at &lt;= #{beforeTime}
        ORDER BY scheduled_at ASC
    </select>

</mapper>
