<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImFileMapper">

    <select id="countAccessibleByConversation" resultType="long">
        SELECT COUNT(DISTINCT m.id)
        FROM im_message m
        JOIN im_conversation_user cu ON cu.conversation_id = m.conversation_id
        JOIN im_conversation c ON c.id = m.conversation_id
        WHERE m.file_id = #{fileId}
          AND cu.user_id = #{userId}
          AND cu.deleted = 0
          AND c.status = 1
    </select>
</mapper>
