<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImConversationMapper">

    <select id="selectConversationPageByUserId" resultType="com.echoim.server.vo.conversation.ConversationItemVo">
        SELECT c.id AS conversationId,
               c.conversation_type AS conversationType,
               c.conversation_name AS conversationName,
               c.avatar_url AS avatarUrl,
               c.last_message_preview AS lastMessagePreview,
               c.last_message_time AS lastMessageTime,
               cu.unread_count AS unreadCount,
               cu.is_top AS isTop
        FROM im_conversation_user cu
        JOIN im_conversation c ON c.id = cu.conversation_id
        WHERE cu.user_id = #{userId}
          AND cu.deleted = 0
          AND c.status = 1
        ORDER BY cu.is_top DESC, c.last_message_time DESC, c.id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countConversationByUserId" resultType="long">
        SELECT COUNT(1)
        FROM im_conversation_user cu
        JOIN im_conversation c ON c.id = cu.conversation_id
        WHERE cu.user_id = #{userId}
          AND cu.deleted = 0
          AND c.status = 1
    </select>

    <select id="selectSingleConversationByBizKey" resultType="com.echoim.server.entity.ImConversationEntity">
        SELECT id,
               conversation_type AS conversationType,
               biz_key AS bizKey,
               biz_id AS bizId,
               conversation_name AS conversationName,
               avatar_url AS avatarUrl,
               last_message_id AS lastMessageId,
               last_message_preview AS lastMessagePreview,
               last_message_time AS lastMessageTime,
               status,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM im_conversation
        WHERE conversation_type = 1
          AND biz_key = #{bizKey}
        LIMIT 1
    </select>

    <update id="updateLastMessage">
        UPDATE im_conversation
        SET last_message_id = #{lastMessageId},
            last_message_preview = #{lastMessagePreview},
            last_message_time = NOW()
        WHERE id = #{conversationId}
    </update>
</mapper>
