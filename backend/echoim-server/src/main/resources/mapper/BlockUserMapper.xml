<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImBlockUserMapper">

    <select id="selectByUserAndBlocked" resultType="com.echoim.server.entity.ImBlockUserEntity">
        SELECT id, user_id AS userId, blocked_user_id AS blockedUserId, created_at AS createdAt
        FROM im_block_user
        WHERE user_id = #{userId}
          AND blocked_user_id = #{blockedUserId}
        LIMIT 1
    </select>

    <select id="selectBlockedItemsByUserId" resultType="com.echoim.server.vo.block.BlockedUserItemVo">
        SELECT b.id AS blockId,
               u.id AS userId,
               u.user_no AS userNo,
               u.nickname AS nickname,
               u.avatar_url AS avatarUrl,
               b.created_at AS blockedAt
        FROM im_block_user b
        JOIN im_user u ON u.id = b.blocked_user_id
        WHERE b.user_id = #{userId}
        ORDER BY b.created_at DESC
    </select>

    <select id="selectBlockedUserIdsByUserId" resultType="long">
        SELECT blocked_user_id
        FROM im_block_user
        WHERE user_id = #{userId}
    </select>

</mapper>
