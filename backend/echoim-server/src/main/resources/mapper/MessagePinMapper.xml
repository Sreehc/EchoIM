<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImMessagePinMapper">

    <select id="selectPinsByConversationId" resultType="com.echoim.server.entity.ImMessagePinEntity">
        SELECT id, conversation_id, message_id, group_id, pinned_by_user_id, created_at
        FROM im_message_pin
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at DESC
    </select>

    <select id="selectPinnedMessageIdsByConversationId" resultType="java.lang.Long">
        SELECT message_id
        FROM im_message_pin
        WHERE conversation_id = #{conversationId}
    </select>

</mapper>
