<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echoim.server.mapper.ImSystemNoticeMapper">

    <select id="selectNoticePage" resultType="com.echoim.server.entity.ImSystemNoticeEntity">
        SELECT id, title, content, notice_type AS noticeType, target_user_ids AS targetUserIds,
               status, published_by AS publishedBy, published_at AS publishedAt,
               created_at AS createdAt, updated_at AS updatedAt
        FROM im_system_notice
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY published_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countNotices" resultType="long">
        SELECT COUNT(1) FROM im_system_notice
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
